# 2026-05-24 作業記録

## 概要

`docs/spec/system-overview.md` を起点に、周辺仕様・ガイド・候補資料の整合性を確認した。

## 実施内容

- MoChat を初期チャット基盤として明確化し、Slack 動作環境仕様を確定仕様から候補資料へ移動した。
- `system-overview.md` の実装モジュール境界を `src/debate/*`、`src/mochat/client.py` に寄せて整理した。
- 旧内部名を `mediator` に寄せ、表示名の「メドラー」と分離した。
- Chutes の Base URL と DeepSeek モデル名を、公開情報に合わせて `https://llm.chutes.ai/v1` と `deepseek-ai/DeepSeek-V3.2-TEE` 前提に修正した。
- 文字化けしていた語句と、旧ロール名が混ざっていた候補資料を修正した。
- リポジトリ配下にあった MoChat 認証情報ファイルを `.gitignore` 済みの `config/local/mochat-credentials.md` に退避した。

## 注意点

- 退避した MoChat トークンは `config/local/mochat-credentials.md` にある。`config/local/` は commit 対象外だが、必要なら MoChat の `rotateToken` で更新する。
- Chutes の thinking モード有効化方法は未実機確認。実装前に最小リクエストで確認する。
